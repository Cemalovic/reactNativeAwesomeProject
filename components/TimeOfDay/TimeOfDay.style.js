import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  text: {
    padding: 50,
    fontSize: 40,
    color: 'green',
    textAlign: 'center',
  },
});

export default style;
